import { createabmaConfig } from "abma-agent-playwright-config/config";

export default createabmaConfig({
  // Add your custom playwright configuration overrides here
  // Example:
  // timeout: 60000,
  // use: {
  //   baseURL: 'http://localhost:3000',
  // },
});
